using System;

namespace fundamentals
{
    class Program
    {
        static void Main(string[] args)
        {
            var notaDigitada = Console.ReadLine();

            var nota = int.Parse(notaDigitada);

            if (nota >= 0)
            {

            }

            // Arrays

            var numbers = new int[] { 1, 2, 3, 4, 5, 6, 7, 8, 9 };
            var numberCopy = new int[10];

        }
    }
}